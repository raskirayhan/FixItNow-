# Frontend Environment Variables
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_your-stripe-publishable-key
# Leave blank for local dev (Vite proxy handles /api). Set to backend host for production.
VITE_API_URL=
